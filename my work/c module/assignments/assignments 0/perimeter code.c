#include<stdio.h>

int main()
{
    int length = 20;
    int width = 10;
    int perimeter;

    perimeter =2 * (length + width);

    printf("Perimeter = %d", perimeter);

    return 0;
}